---
description: Create a git commit with a short single-line message
---
Look at the staged and unstaged changes using `git diff` and `git status`.

Create a SHORT single-line commit message (no body). Keep it under 72 characters.
Run: `git commit -m "<message>"`

IMPORTANT: Before running git commit, show me the proposed commit message and ask for confirmation.
