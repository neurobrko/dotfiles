---
description: Create a git commit with subject line and descriptive body
---
Look at the staged and unstaged changes using `git diff` and `git status`.

Create a short subject line (under 72 characters) AND a body with a brief description of the changes.
Run: `git commit -m "<subject>" -m "<body>"`

IMPORTANT: Before running git commit, show me the proposed commit message and ask for confirmation.
